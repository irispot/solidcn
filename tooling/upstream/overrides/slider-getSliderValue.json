{
  "schemaVersion": 1,
  "purpose": "Use native-local dependencies in getSliderValue. No algorithm edits.",
  "replacements": [
    {
      "find": "import { clamp } from '@base-ui/utils/clamp';",
      "replace": "import { clamp } from './clamp';",
      "expectedOccurrences": 1
    }
  ]
}
